# SSL certificates (self-signed for development)
certs/

# Persisted settings (auto-generated)
server/data/settings.json

.DS_store

# Per-worktree port overrides (not committed — each worktree sets its own)
**/.env.local